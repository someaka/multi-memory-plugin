name: multi-memory
version: 0.2.0
description: |
  Run multiple memory providers (Mnemosyne, Mem0, Holographic, Honcho)
  simultaneously via a single MemoryProvider instance. Configured via
  memory.multi.backends in config.yaml.
pip_dependencies: []
