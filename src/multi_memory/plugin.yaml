name: multi
version: 0.10.0
description: |
  Run multiple memory providers simultaneously (mnemosyne, mem0, holographic,
  honcho, openviking, hindsight, retaindb, byterover, supermemory).
  Fan-out lifecycle calls with per-provider error isolation.
kind: standalone
pip_dependencies: []
